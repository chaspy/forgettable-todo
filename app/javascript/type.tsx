export type Response = {
    user_id: Number
    title: string
    description: string
    finished: Boolean
    created_at: Date
    updated_at: Date
}
export type Request = {
    user_id: Number
    title: string
    description: string
    finished: Boolean
}
